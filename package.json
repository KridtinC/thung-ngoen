{
  "name": "thung-ngoen",
  "version": "1.3.6",
  "main": "server.ts",
  "dependencies": {
    "@elysiajs/static": "^1.2.0",
    "elysia": "^1.2.14",
    "mongoose": "^8.10.1"
  },
  "description": "LIFF app and LINE chatbot for splitting bills",
  "scripts": {
    "build:client": "bun build src/client/index.ts --outfile public/app.js --target browser --minify",
    "dev": "bun build src/client/index.ts --outfile public/app.js --target browser --watch & bun --watch server.ts",
    "start": "bun run build:client && NODE_ENV=production bun server.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^6.0.3"
  }
}
